services:
  app:
    build: .
    ports:
      - "8080:80"
    env_file:
      - .env
    environment:
      APP_PASSWORD: ${APP_PIN}
    volumes:
      - sqlite_data:/var/www/html/data
    restart: unless-stopped

volumes:
  sqlite_data:
